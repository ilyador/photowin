type Set @model {
  id: ID!
  user: String!
  pictures: [Picture!]! @connection
}

type Picture @model {
  id: ID!
  file: S3Object!
  rating: Int
  appearedForRanking: Int
}

type S3Object {
  bucket: String!
  key: String!
  region: String!
}
